{
  "quadratic": {
    "triggers": [
      "quadratic",
      "quadratic equation",
      "quadratic formula"
    ],
    "steps": [
      "Compute discriminant: {b}² - 4{a}{c}",
      "Compute square root of {theDiscriminant}",
      "Compute (-{b} + √{theDiscriminant}) / 2{a}",
      "Compute (-{b} - √{theDiscriminant}) / 2{a}"
    ],
    "command": "quadraticEquation"
  },
  "sineRuleAngle": {
    "triggers": [
      "sine angle",
      "sin angle",
      "arcsin"
    ],
    "steps": [
      "Convert Angle A ({a}) to Radians",
      "Compute sin(A): sin({a})",
      "Compute (b * sin(A)) / a → ({b} * sin({a})) / {c}",
      "Compute arcsin(value) to get Angle B",
      "Convert Angle B to Degrees"
    ],
    "command": "sineRuleAngle"
  },
  "sineRuleSide": {
    "triggers": [
      "sine side",
      "sin side"
    ],
    "steps": [
      "Covert Angle A ({a}) and ({b}) to Radians.",
      "Compute sin(A) and sin(B): sin({a}) | sin({b})",
      "Compute (a * sin(B) / sin(A) → ({c} * sin({b}) / sin({a})",
      "Simplify."
    ],
    "command": "sineRuleSide"
  },
  "cosineRuleAngle": {
    "triggers": [
      "cosine angle",
      "cos angle"
    ],
    "steps": [
      "Fill in all known information, squaring the top.",
      "Solve numerator.",
      "Solve denominator.",
      "Angle (Radians) from arccos."
    ],
    "command": "cosineRuleAngle"
  },
  "cosineRuleSide": {
    "triggers": [
      "cosine side",
      "cos side"
    ],
    "steps": [
      "Convert Angle C ({a}) to Radians.",
      "Compute cos(C): cos({C})",
      "Compute a^2 = b^2 + c^2 - 2bccos(C): a^2 = {b}^2 + {c}^2 -2({b})({c})cos({a})",
      "Simplify to get a^2.",
      "Square root the answer to get a."
    ],
    "command": "cosineRuleSide"
  }
}